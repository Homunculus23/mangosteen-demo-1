import { defineComponent, ref } from "vue";

export const StartPage = defineComponent({
    setup: (props, context) => {
        return () => (
            <div>
                start页面
            </div>
        );
    }
})